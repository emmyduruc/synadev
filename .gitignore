# Accidental Expo prebuild at repo root — use client/ios instead
/ios
/android
/app.json

node_modules
dist
.turbo
.expo
.expo-shared
*.log
.DS_Store

# Secrets — keep templates tracked
.env
.env.local
.env.*.local
!.env.example
!**/.env.example

coverage
*.tsbuildinfo
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.cursor
.vscode
.idea
